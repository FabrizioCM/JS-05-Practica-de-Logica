//Estructura de lo que tengo que hacer
let arregloMultidimensional = [1, [2, 3, [4, 5, [6]]]];
let arregloArreglado = arregloMultidimensional.flat(Infinity);
        console.log(`Tu arreglo final es: ${arregloArreglado}`);
